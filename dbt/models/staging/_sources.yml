version: 2

sources:
  - name: bronze
    description: ステージングモデルが参照する Hub Lakehouse。
    database:　demo_dbt_hub
    schema: dbt
    tables:
      - name: customers
        description: Bronze 層の顧客マスタ。
      - name: orders
        description: Bronze 層の注文データ。
